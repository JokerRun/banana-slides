# Legacy Style Preset Assets

This directory is kept only for backward-compatible repository layout. New
runtime flows must load product presets from `assets/presets/<preset-id>/`.

DDI runtime source of truth:

```text
assets/presets/ddi/
  preset.json
  base.png
  prompt-generate.md
  prompt-restyle.md
  prompt-translate-restyle.md
```
