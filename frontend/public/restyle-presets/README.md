# Legacy Public Restyle Presets

This directory is retained for compatibility with older builds. Runtime product
flows must use backend preset metadata and image URLs from `/api/presets`.

DDI source of truth is `assets/presets/ddi/`.
